---
import { languages } from "../../../i18n/ui";
import StaffPage from "../../../components/pages/intro/Staff.astro";

export function getStaticPaths() {
    return Object.keys(languages)
        .map((lang) => {
            if (lang === "ko") return undefined;
            return { params: { lang } };
        })
        .filter(Boolean);
}
---

<StaffPage />
