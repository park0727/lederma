---
import { languages } from "../../../i18n/ui";
import EquipmentPage from "../../../components/pages/intro/Equipment.astro";

export function getStaticPaths() {
    return Object.keys(languages)
        .map((lang) => {
            if (lang === "ko") return undefined;
            return { params: { lang } };
        })
        .filter(Boolean);
}
---

<EquipmentPage />
