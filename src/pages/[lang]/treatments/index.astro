---
import { languages } from "../../../i18n/ui";
import Treatments from "../../../components/pages/treatments/Index.astro";

export function getStaticPaths() {
    return Object.keys(languages)
        .map((lang) => {
            if (lang === "ko") return undefined;
            return { params: { lang } };
        })
        .filter(Boolean);
}
---

<Treatments />
